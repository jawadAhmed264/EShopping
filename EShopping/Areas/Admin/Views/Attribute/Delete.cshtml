@using EShopping.Areas.Admin.Models.AttributeViewModels
@model AttributeViewModel

@{
    ViewBag.Title = "Delete";
}

<main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
    <div class="main">
        <div class="col-lg-12">
            <div class="inline">
                <div class="col-lg-6">
                    <h1>Delete Attribute</h1>
                </div>
            </div>
        </div>
        <!--Form-->
        <br>
        <div class="col-lg-12">
            <h3>Are you sure you want to delete Attribute @Model.AttributeName?</h3>
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(m => m.Attribute_Id)

                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-outline-danger" /> |
                    <a class="btn btn-outline-dark" href="@Url.Action("Index")">Back to List</a>
                </div>
            }
        </div>
    </div>

</main>
